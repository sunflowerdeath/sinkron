{
    "name": "sinkron-tauri",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "start": "tauri dev",
        "macos:build": "tauri build --target aarch64-apple-darwin",
        "macos:build:debug": "tauri build --target aarch64-apple-darwin --debug",
        "android:dev": "tauri android dev",
        "android:build": "tauri android build --split-per-abi --target aarch64 --apk"
    },
    "dependencies": {
        "@tauri-apps/api": "^2.0.2"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^2.0.3"
    }
}
